import { styled } from '@mui/material';

export const WrapperTaskBox = styled('div')(() => ({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  rowGap: 16,
}));

export const WrapperForTasksTaskBox = styled('div')(() => ({
  display: 'flex',
  flexDirection: 'column',
  rowGap: 16,
  width: '100%',
}));
