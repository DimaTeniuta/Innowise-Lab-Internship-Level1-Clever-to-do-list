import { createTheme } from '@mui/material';

export const themeBlue = createTheme({
  palette: {
    primary: {
      main: '#0288d1',
    },
    secondary: {
      main: '#e3f2fd',
      contrastText: '#153551',
    },
  },
});
