import { createTheme } from '@mui/material';

export const themeBlue = createTheme({
  palette: {
    primary: {
      main: '#fefefe',
      contrastText: '#000',
    },
    secondary: {
      main: '#ffa726',
      contrastText: '#153551',
    },
  },
});
